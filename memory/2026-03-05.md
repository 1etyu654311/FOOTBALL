# Memory: 2026-03-05

## OpenClaw model routing updates (Groq + fallback)

- User requested OpenClaw-level model configuration (not project script-level).
- Added Groq provider in OpenClaw config with models:
  - `groq/llama-3.3-70b-versatile`
  - `groq/llama-3.1-8b-instant`
  - `groq/openai/gpt-oss-120b` (catalog entry)
- Added aliases under `agents.defaults.models`:
  - `groq70` -> `groq/llama-3.3-70b-versatile`
  - `groq8` -> `groq/llama-3.1-8b-instant`
  - `codex` -> `openai-codex/gpt-5.3-codex`

## Switching behavior requested by user

- User wanted manual switching from Telegram and automatic fallback.
- Config now supports switching primary model and has ordered fallbacks.
- At one stage user switched back to Codex; later requested Groq again.

## 400 error diagnosis and mitigations

- User reported recurring: `400 Please reduce the length of the messages or completion` when using Groq.
- Root cause confirmed: oversized context / token pressure on Groq requests (also observed 413 in direct stress test with very large prompt).
- Applied OpenClaw config mitigations:
  - Reduced per-model `max_tokens` params for Groq aliases/models.
  - Increased compaction aggressiveness (`safeguard` + higher reserve floor + lower history share).
  - Lowered effective context budget (`contextTokens`) to reduce oversized payload risk.
  - Kept fallback chain to Codex and Groq8 for resilience.

## Validation

- Restart after each config.patch succeeded.
- Direct Groq stress test succeeded at safe size (`200` + `STRESS_OK`) and failed only when request was intentionally too large (expected behavior).

## Multi-agent architecture decision (cost control)

- User wants a separate independent OpenClaw agent with its own Telegram bot and model routing.
- Core strategy agreed:
  - Main agent (Codex/strong model) reserved for planning/review.
  - Worker agents use cheaper models for repetitive execution.
  - Each agent should support primary + fallback model chain.
  - Frequent reminders/watchdog jobs are required because worker runs may stop unexpectedly.
- Safety requirement from user: do not do risky broad config edits; proceed with backup + minimal patch approach.
- I provided rollback-first commands and user created a backup successfully.

## New agents + routing implemented

- Added agent `worker-glm` with model chain:
  - primary: `bigmodel/glm-4.7-flash`
  - fallbacks: `bigmodel/glm-4` -> `openai-codex/gpt-5.3-codex`
- Enabled cross-agent coordination and visibility:
  - `tools.agentToAgent.enabled = true`
  - allowlist includes `main`, `worker-glm`, `worker-litellm`
- Added agent `worker-litellm` with dedicated LiteLLM provider.

## LiteLLM migration to GLM5

- User provided old LiteLLM snippet using model alias `kimi`.
- Updated `/root/litellm.yaml` so previous `kimi` mapping points to GLM5 backend via NVIDIA endpoint, then made alias clearer by adding `glm5` model name.
- Updated OpenClaw config for litellm provider model id to `litellm/glm5` and switched `worker-litellm` primary model to `litellm/glm5`.
- User confirmed it works after restart and now runs successfully.

## 3-hour project delegation + reminder strategy

- User requested a 3-hour execution sprint for creating a multilingual football news blog (EN/ES/FR/AR), attractive UX, and GitHub publish.
- Launched long-running delegated task under `worker-litellm` (label: `football-blog-3h-build`) with strict small-task execution protocol and status file updates.
- Added automation reminders:
  - periodic continue-reminder every 10 min for worker
  - hourly progress announcement to Telegram
  - final 3-hour delivery checkpoint announcement
- User concern: sub-agent reminders may be unreliable.
- Added 4 one-shot monitoring reminders to main agent at +10/+20/+30/+40 min so main agent actively supervises worker continuity.

